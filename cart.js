// cart.js — restored no-op placeholder (restored after accidental deletion)
// This file was removed earlier per user request; recreating a harmless placeholder
// so that VS Code's Run/Play command that references files will find it.

// If you want to implement cart functionality later, replace this content.

(function() {
  // no-op
  if (typeof window !== 'undefined') {
    window.JadulaCart = window.JadulaCart || { items: [] };
  }
})();
